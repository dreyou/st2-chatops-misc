---
    name: "run_logcheck_chain"
    runner_type: "action-chain"
    description: "Run logcheck chain action"
    enabled: true
    entry_point: "logcheck_chain.yaml"
    parameters:
      hosts:
        type: "string"
      match:
        type: "string"
      num:
        type: "string"
      log:
        type: "string"
      skip_notify:
        default:
          - c1
